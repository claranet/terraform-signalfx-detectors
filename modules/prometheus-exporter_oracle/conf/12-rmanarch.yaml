module: oracle
name: Oracle_RMAN_ARCH

transformation: true
aggregation: true

signals:
  signal:
    metric: oracledb_Oracle_RMAN_Arch_value   

rules:
  critical:
    threshold: 0
    comparator: ">"
    lasting_duration: "60m"
