## Example
module: pm2
name: heartbeat

transformation: false
aggregation: true
filtering: "(not filter('name', 'pm2-metrics'))"
exclude_not_running_vm: true

signals:
  signal:
    metric: "pm2_up"
rules:
  critical:
