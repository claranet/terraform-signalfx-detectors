module: zookeeper
name: health
disabled: false
signals:
  signal:
    metric: "gauge.zk_service_health"
rules:
  critical:
    threshold: 1
    comparator: "!="
    description: "is not running"
    lasting_duration: "5m"
    health_disabled: "false"