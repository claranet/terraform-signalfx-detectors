module: "GCP Firebase database"
name: "Firebase database load"

transformation: ".min(over='30m')"

signals:
  signal:
    metric: "io/database_load"

rules:
  critical:
    threshold: 10
    comparator: ">"

  major:
    threshold: 5
    comparator: ">"
    dependency: "critical"
