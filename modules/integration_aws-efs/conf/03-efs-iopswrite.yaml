module: "AWS EFS"
name: "IOPs Write Stats"
filtering: "filter('namespace', 'AWS/EFS')"
value_unit: "IOPs"
transformation: ".max(over='15m')"
signals:
  data_write_io_bytes:
    metric: "DataWriteIOBytes"
    filter: "filter('stat', 'mean')"
  meta_data_io_bytes:
    metric: "MetadataIOBytes"
    filter: "filter('stat', 'mean')"
  signal:
    formula:
      (meta_data_io_bytes/data_write_io_bytes).scale(100)
rules:
  critical:
    comparator: ">"
  major:
    comparator: ">"
    dependency: critical