module: Azure VM backup
name: success
id: vm_backup
transformation: true
aggregation: true

signals:
  signal:
    metric: fame.azure.backup.vm
    extrapolation: 'zero'

rules:
  critical:
    threshold: 1
    comparator: "<"
    lasting_duration: '25h'
