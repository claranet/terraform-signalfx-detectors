module: redis
name: stored keys change rate

filtering: "filter('plugin', 'redis_info')"

signals:
  signal:
    metric: gauge.db0_keys
    function: ".rateofchange()"

rules:
  major:
    threshold: 0
    comparator: '=='
    lasting_duration: 1h
