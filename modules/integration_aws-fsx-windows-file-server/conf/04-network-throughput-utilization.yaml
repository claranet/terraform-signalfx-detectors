module: "AWS FSx for Windows File Server"
name: "Network Throughput Utilization"
filtering: "filter('namespace', 'AWS/FSx')"
value_unit: "%"
transformation: ".max(over='15m')"
signals:
  signal:
    metric: "NetworkThroughputUtilization"
    filter: "filter('stat', 'mean')"
rules:
  major:
    comparator: ">"
    threshold: 60
  critical:
    comparator: ">"
    threshold: 80
    dependency: major
